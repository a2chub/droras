# AGENTS 指示

## 目的
このファイルでは、プロジェクト開発を安定させるための作業手順と
コードスタイルに関するガイドラインを定義します。

## 基本的なワークフロー
1. 環境管理には [rye](https://rye-up.com/) を利用し、 **Python 3.12** を使用してください。
2. `front/` ディレクトリでは **pnpm** を使用します。
3. 小さく焦点を絞ったコミットを作成し、英語で簡潔にメッセージを書いてください。
4. コミット前に下記のプログラムチェックを実行します。

## プログラムチェック
- **Python の整形と lint**
  - 依存関係をインストールするため `rye sync` を実行します。
  - `black` と `isort` で整形を行います。
  - `flake8` で lint を行います。
- **フロントエンドのチェック**
  - `front/` で一度 `pnpm install` を実行した後、`pnpm lint` を実行します。
  - `pnpm build` がエラーなく完了することを確認してください。

現在このプロジェクトには自動テストがありません。テストが追加された場合は
`pytest` を実行してください。

## コードスタイル
- 新しい Python 関数には型ヒントを付け、1 行は **88** 文字以内に抑えてください。
- JavaScript / TypeScript は `front/` の ESLint ルールに従います。

## プルリクエストの指針
- 変更内容を 2~3 行で簡潔にまとめてください。
- 変更を確認するために必要な手動手順があれば記載してください。

## 補足
ガイドラインは随時更新される可能性があります。最新の内容はこのファイルを参照してください。
